"use client";import {useState}from"react";import{Button}from"@/components/ui/Button";import{useToast}from"@/components/ui/Toast";import{assignWorkdayAction}from"@/lib/actions/workday";export function AssignButton({simulationId,applicationId,candidateId}:{simulationId:string;applicationId:string;candidateId:string}){const[busy,setBusy]=useState(false),{showToast}=useToast();return <Button size="sm" variant="secondary" disabled={busy} onClick={async()=>{setBusy(true);try{await assignWorkdayAction(simulationId,applicationId,candidateId);showToast("Digital Workday assigned.","success")}catch(e){showToast(e instanceof Error?e.message:"Assignment failed.","danger")}finally{setBusy(false)}}}>{busy?"Assigning…":"Assign"}</Button>}
